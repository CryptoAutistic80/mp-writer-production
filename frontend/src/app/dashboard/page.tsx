export default function DashboardPage() {
  // AnimatedBackground is rendered globally in layout.tsx.
  return (
    <main className="hero-section">
      <section className="card">
        <div className="container">
          {/* Intentionally blank dashboard shell */}
        </div>
      </section>
    </main>
  );
}

